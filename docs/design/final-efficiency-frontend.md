# Final frontend efficiency implementation

Task FINAL-EFFICIENCY-20260912; baseline e32c13c5b04c3d5db1b60637be115c2b61fcfb78; branch codex/final-resource-efficiency-20260912. Root task record is present; shared worktree contains parallel backend edits. Root/frontend AGENTS and ADR0001/0005/0006 govern. Pro audit PERF04/09 agrees with concrete portable build/admission evidence; its architectural/security removal alternatives are not blanket authorization.

## Before-edit slice A: F08–F11

- Targets: portable-runtime-worker-client.request and protocol clone helper/tests; workspace-projection ConversationProjection/tests; LiveChatSession draft setter/send and confirmed-deletion cleanup/tests, WorkspaceApp wiring where required; WorkspaceApp stable display-mode derived input and chat-display tests.
- Public entry points remain worker client/WorkspaceApp/LiveChatSession; no IPC, dependency, state budget, sorting or capability changes.
- F08: perform one bounded normalization/serialization before structural validation, preserving protocol-error code and receiver checks. Test proxy/oversize/cycle behavior and encoder count.
- F09: cache branch views by immutable branch-array identity; invalidate on new branch array and scope. Preserve ordering, title fallback, and saved-message reuse.
- F10: delete empty draft entries; prune only authoritative deleted scopes. Keep unsent nonempty drafts and late-send/newer-typing guards. No arbitrary count eviction.
- F11: isolate display-mode resolution from raw stream updates. Preserve preference changes, room/native-mode changes, and compatibility fallback.
- Expected size: small fields/methods/helpers, no cap increases. Risks: stale branch titles, proxy error handling, accidental loss of drafts, stale preferences. Targeted baseline is run before edits and results recorded below.

## Before-edit slice B: F05/F06 / Pro PERF04/09

- Targets: PortableMessage reactive build lifecycle/buildPortableDocument/asset-reference lookup, private document scheduler and asset resolver helpers/tests, asset-delivery-loader reuse; media visibility helpers as justified. Native descriptor/image work is owned by asset agent and contracts coordinated with root.
- Preserve source ordering, Unicode selection, sanitizer/CSP/origin/runtimeId/action checks, 4-slot admission, cancellation, approved descriptors, 128 references, width-triggered rebuild, room BGM. Deduplicate by selected asset ID within a build; retry only explicit transient failures with bounded attempts and AbortSignal. Coalesce builds and retain latest work, no unbounded promise chain; unchanged documents must retain valid listener identity.
- Expected size: extract private helpers as needed under PortableMessage cap. Risks: old build publishing, listener removal with retained srcdoc, permanent errors retried, approved room media silently disabled. Baseline portable/asset tests precede implementation.

Slice A/B baseline: 6 files / 44 tests PASS, Lua sandbox and regex worker posttests PASS. F10 confirmed deletion cleanup is deferred: no renderer deleteConversation/deleteBranch success contract exists. Full catalog absence is not used as deletion proof because stale catalog results can race newly created unsent drafts. Empty entries and successful sends now remove keys; all nonempty drafts remain.

## Before-edit slice C: conditional historical media

- Targets: private portable media visibility parent helper/test; PortableMessage frame lifecycle; trusted bridge incoming visibility handling/test; sanitizer post-normalization media attributes/test. No generic/native IPC or imported script admission.
- Message surfaces only: validated audio autoplay intent becomes an internal marker, preload is none, images use lazy/asynchronous decoding; room attributes/BGM remain unchanged. The trusted parent reports viewport/document visibility to its exact iframe; bridge accepts only parent source + channel + current runtime ID + boolean and pauses/resumes only historical media it paused. Intentional user pauses are not undone. One observer/listener set is destroyed with its frame. Native image/pixel caps remain asset-agent owned; derivative/codec endpoint is not invented.
- Tests: initial hidden/no preload autoplay, viewport hide/resume, stale/spoof visibility ignored, room BGM unaffected, observer cleanup. Preserve CSP/sandbox/action and approved media URLs.

## Completed and verified

- F08: sender now performs one bounded clone/normalization before structural validation. Proxy request test observes one TextEncoder call; receiver limit/shape validation remains.
- F09: unchanged branch-array identity reuses the branch view array. A 500-branch / 1,000 projection regression observes zero additional maps; changed/reordered titles invalidate it.
- F10: empty and successfully sent draft keys are removed, including 500 scope cycles. Nonempty unsent drafts are retained. Confirmed deleted-scope pruning is declined for now: this renderer has no authoritative conversation/branch deletion success API, and catalog absence can race a new scope.
- F11: one-entry per-workspace display preference resolver caches missing storage values and invalidates on conversation/native mode/preference identity. 1,000 unchanged calls read storage once; the actual WorkspaceApp streaming test observes no extra selected-conversation fallback reads.
- F05/F06 and Pro PERF04/09: per-build selected asset IDs share promises and the existing global four-slot admission. Explicit recoverable storage_unavailable alone retries, at most eight attempts for portable builds; cancellation interrupts queue/waits. No cross-build approval cache. 128 aliases resolve once per build; the next build verifies again. The queue holds one running and one latest replacement; a 200-update test invokes only the initial and final build. Sanitized content/style plus action/scope identity determine srcdoc replacement. Same-content rebuild retains the existing bridge ID and action acceptance; stale/cancelled builds never publish.
- Historical message audio has preload none and autoplay controlled by exact-parent/channel/runtime-ID/boolean visibility messages. Offscreen or hidden-document media pauses; only visibility-paused playback resumes, including delayed play completion. Imported message images use lazy/async attributes. Room BGM attributes and playback are unchanged. Media observer teardown is independent from the document identity listener, so an unchanged Svelte prop publication cannot clear a retained iframe's bridge state.
- Thumbnail extension decision: existing 64 MiB / 64-image / 30-second retention and shared four-slot priority admission stay. Native image pixel validation is handled in the asset slice. Existing descriptor dimensions are not verified actual dimensions, so no decoded-byte pre-reservation trusts them. No derivative endpoint, recompression dependency, or invented thumbnail contract was added. Animation-total-frame memory remains a native/browser limitation rather than a claimed frontend guarantee.

Validation: 11 focused files / 81 tests PASS; scoped ESLint and Prettier PASS. Final TypeScript/Svelte PASS (0 errors/warnings), i18n baseline PASS, Lua sandbox and regex worker posttests PASS. Architecture run stopped on parallel Storage private_path/tests.rs cfg(test) classification before its final size report; parent owns full gate. Measured frontend production files remain under existing limits: WorkspaceApp 599 lines/25,790 bytes; PortableMessage 536/20,307; renderer policy 586/16,593; bridge 212/9,115. No caps, dependencies, generic IPC, CSP, sanitizer URL/attribute allowlist, runtime budgets, or durable stream ordering were weakened.

## Follow-up before edit: render-only last-message index

Target PortableMessage document identity and its focused tests. lastMessageId is a macro/display transform input, not an action-route identity: the parent handler passes only the validated action string to the current runtime; runtime.handleAction captures its current worker context and checks open/version authority. Keep lastMessageId as a reactive build input, but remove it from the identity tuple so equal sanitized content/style can retain srcdoc. Frame/profile/client/surface/messageIndex identities remain. Tests reproduce lastMessageId 1→2 with identical markup and verify the existing bridge action still works, then verify lastmessageid macro changes rotate/reject old identity. No scope/grant/CSP/policy change; expected size minus one production line and focused test additions.

Follow-up result: existing 23 component tests PASS before editing; the added lastMessageId-only regression failed against the old identity tuple. Removing only that identity entry makes PortableMessage + queue 28 tests PASS, including unchanged srcdoc/action identity and changed macro content rejecting the previous identity. Scoped Prettier and ESLint PASS. lastMessageId still participates in every relevant display/macro build, so this removes redundant publication, not evaluation or authority checks.

## Follow-up before edit: branch selection paints its first page

Target ConversationController.selectBranch and a focused app-controller branch paging test. Baseline remains FINAL-EFFICIENCY-20260912; shared dirty branch includes parallel Rust journal work. Keep selectBranch IPC ordering, epoch authority, loading mutation gate, ready publication, retry refresh and generation resume unchanged. After the selection response, validate epoch before reading bodies; reuse loadRecentBranchMessages onInitial to publish 30 messages with the selected state while phase remains loading, then retain existing 128-context completion. Tests: slow context shows first30; late select response skips all body requests; late first page cannot publish/start128; late context cannot replace a newer branch. Expected production delta ~20 lines inside existing <900-line controller, no APIs/dependencies/other controller behavior changes. Full frontend check/test(maxWorkers4)/build follows targeted validation; no Cargo.

Branch-first-page result: existing 2 files/20 tests PASS before edits; new regression reproduced missing first30 and stale-selection unnecessary128 query. Updated 3 focused files/24 tests PASS. Controller is 623 lines/25,640 bytes (under existing implementation cap). Final Node24 full check PASS (format/i18n/lint/TypeScript/Svelte 0 errors/warnings); full Vitest with maxWorkers4: 156 files/842 tests PASS; Lua/regex posttests PASS; production build PASS (existing >500kB bundle warning retained). Logs: /tmp/lorepia-final-frontend-20260912/check.log, test.log, build.log. No Cargo executed for this slice.
