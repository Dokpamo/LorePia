# REVIEW-FIX-20260908 — creator and memory pagination

Baseline: snapshot `3c39090df2951673d4cc2c1ce044830b8287c3c5`; original/merge-base `6f22761a4305443541452e7409639114f4c10dfd`.
Worktree: `/Users/codexer/.codex/worktrees/lorepia-review-fixes-20260908`, initially clean snapshot including the user's existing changes. Original checkout is not modified.
Authorized scope: review findings 4–6; creator documents beyond 100, aggregate IPC limits before filtering, memory records beyond 250. User explicitly authorized needed API/IPC behavior extensions. No dependency or schema changes.

Targets and public entries: Storage orchestration document and memory read paths; Core facade; Shell API orchestration DTOs; Tauri typed commands; generated IPC registry and both capabilities; frontend IPC client and creator/memory controllers/views. New small pagination modules contain added logic. Existing list and mutation entries remain supported. Symbols to move: bounded creator list projection and legacy list adapters into a child module if parent size requires it. Shared ownership: other agents own provider loading, asset protocols, stream history and policy ratchets.

Invariants: Storage alone owns SQLite transactions; live ID keyset cursor ordering; edits and deleted anchors do not invalidate continuation; refreshing includes new IDs inserted before the cursor; branch-visible memory and exact mutation revisions; only UserCreated creator documents cross creator list projection; actual IPC responses retain byte/node/text bounds; no renderer DB rows/paths/credentials; generated IPC surfaces remain exact.
Expected size delta: new focused modules, parent files receive small delegation/registration changes and lose duplicated legacy list bodies; no source cap increases. Semantic risks: live collection changes while paging, incorrect branch visibility, dropped pages, stale context response overwriting edits, aggregate output limits. Task/ADR authority: repository AGENTS.md; user authorization of all review findings; existing persona pagination/cursor pattern.

Inventory: rg identified creator list methods, project_creator_revisions, document_store::list_documents, memory_visibility::list_visible_memory_records, get_orchestration_workspace, context-room-controller and MemorySection. Targeted baseline: creator-document-controller Vitest 3/3 passed using Node 24.18.1; native baseline coordinated with shared Cargo owner. Planned verification: >100 creator documents, imported aggregate >2 MiB excluded before projection, >250 memory records paging/editing, malformed/stale/owner-bound cursors; IPC generator and frontend checks; root performs full gate.

Implemented: two strict commands, `list_creator_documents_page` and `list_memory_records_page`, with count-limited SQL keyset reads and a 512 KiB raw page target. A single larger valid document is still returned; the Shell validates actual projected response bounds before returning. Creator filtering happens in SQL before raw decoding. Core owns its cursor and kind contracts and privately converts to Storage contracts. Existing live settings collection wrappers consume bounded pages; the creator and memory studio presents explicit load-more and refresh actions. Existing mutation revision checks are unchanged. Cursor scope is independent of revisions: unrelated edits, background additions and deletion of the previous anchor remain compatible with continuation. New IDs before the cursor are discovered by refresh.

Focused extractions keep old parents within their existing caps: Storage `StoredRevision` to `orchestration/revision.rs`; creator routes to `creator-document-routes.ts`; memory props to `memory-section-props.ts`; empty workspace constructors and legacy mock/preview creator loader to small controller modules. No unrelated renames or schema changes.

Verification completed: Storage pagination regression (275 records, concurrent edit/addition, 276 reached in strict ID order, final record CAS edit, mismatched visibility cursor rejected); Shell pagination regression (105 user-created 32 KiB books plus 40 imported 60 KiB books, all creator pages and legacy filtered first page succeed, every actual output passes IPC bounds, item 105 edits successfully, malformed/cross-kind cursors reject, edited/deleted anchors continue). Earlier live memory CRUD/reopen regression passed. Targeted frontend creator, memory and editor suite passed 16 tests; additional controller/client suite passed 52 tests, including 275 records, same-room reload epochs, old-context request loading completion, dirty draft preservation beyond the refreshed first page, and live settings loading 105 documents. Whole renderer typecheck reported zero errors (two existing ChatPane capture warnings). Generated page permissions were reviewed and added to the index. Narrow clippy initially reported wildcard imports; pagination imports were made explicit, with the full lint/build gate delegated to the integration owner.
