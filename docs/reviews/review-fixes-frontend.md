# Frontend review fixes preflight

- Task ID: REVIEW-FIX-20260908 (issues 1, 8, 10, 15).
- Baseline: snapshot `3c39090df2951673d4cc2c1ce044830b8287c3c5`; merge-base `6f22761a4305443541452e7409639114f4c10dfd`.
- Worktree: isolated `/Users/codexer/.codex/worktrees/lorepia-review-fixes-20260908`, initially clean; original user checkout is untouched. Other agents own separate review fixes.
- Authorization: user approved all review issues, including necessary behavior, API and policy changes. No dependencies or schema changes are planned here.
- Targets/public entries: PromptTaskController through OrchestrationController; ChatScrollLifecycle through ChatViewport and workspace ChatPage; ProviderController through LorepiaAppController and existing get_provider_overview IPC/native DTO; provider repository read aggregation if needed; UI contract tests.
- Invariants: latest request/context/selection wins, preserved dirty drafts, verified stream ordering and scroll anchors, bounded DOM/date work, stable provider ordering, credential status does not become credential authority, native secrets never cross DTOs, existing IPC command names/capabilities remain exact.
- Symbol/caller inventory: `rg` identified prompt preset loader/save and stageRoomConfig caller, virtualWindow and scroll consumers, provider overview/list routes/list presets and CRUD refresh callers, and all source-pattern UI tests before source reads.
- Symbols remain: existing public controller/IPC methods, date grouping and virtual layout APIs, credential target keys, provider settings serialized mutation authority. New bounded helper files may own provider loading and regression fixtures.
- Baseline tests: 145 passed, 2 failed across 10 focused files. Existing failures are blanket native-select prohibition and a MobileAll direct-import expectation invalidated by the supplied UI composition. Same-day scaling and stale-preset repros were previously independently confirmed. Architecture checker requested before edits; root owns existing architecture baseline drift.
- Relevant tests: ChatPane.scroll, orchestration-controller, provider-selection/discovery/bootstrap, UI controls/mobile flows, styles contracts; new stale-response/call-count/isolated-error regressions; native/Core/Storage provider tests for aggregated reads.
- Expected size delta: prompt loader guard about +20 lines, scroll early exit about +2; provider controller shrinks by extracting the loading owner; source-string test removal exceeds replacement helper size. Existing source caps will not increase; root owns measured ratchets.
- Semantic risks: provider aggregate consistency and sorting; optional diagnostics failure visibility; late credential reads overwriting capture/delete; latest preset identity after save/readback; browser DOM/CSS tests must preserve accessibility and actual boundary checks rather than implementation spelling.
- Governing material: root/frontend/Core/Storage AGENTS.md, ADRs 0001 layer boundaries, 0002 Storage ownership, 0003 credential boundary, 0004 IPC command registry, existing review report `/Users/codexer/.codex/reviews/lorepia-20260908-review.md`.

## Validation and outcome

- Issue 1: preset loads, null selections and saves share a request generation plus context generation. Older success/failure responses cannot restore a previous selection; a draft whose ID differs from the selected preset is never saved. Newer edits to the same draft still survive save readback.
- Issue 8: extending a visible transcript window to the beginning of its calendar day stops at the existing 80-message DOM budget. Date work stays bounded for both 1,000 and 100,000 messages while the existing scroll and anchor logic remains intact.
- Issue 10: the existing provider overview response now includes routes and generation presets. Storage reads both ordered collections with two queries under one connection guard; Core/Shell preserve their domain/DTO boundaries. Startup issues one overview IPC and credential status reads, with no per-route or per-preset IPC fan-out. Catalog, discovery and model-sync diagnostics load when their corresponding live settings panels mount and have independent error/retry authority. Ordinary mutations retain unrelated credential statuses; changed connection/profile records alone refresh their status. Native status read failures become unreadable. Catalog activation/rollback refreshes mounted history and invalidates older diagnostics, and older discovery lists/status reads cannot replace newer session revisions.
- Issue 15: removed native select/option/SVG bans and exact source spelling, component import, pixel, palette value and timing assertions. Replacements render controls/headers/error regions, dispatch actions, check ARIA and actual theme/native synchronization, and parse CSS using Svelte's supported parser for focus, hover media, reduced-motion and semantic token contracts. Existing IPC, capability and native window configuration checks remain.
- Latest focused regression run: 30/30 passed across provider loading (12), preset races (7), scroll scaling (2), and styles (9). Lua timeout sandbox and portable regex worker checks passed. Earlier wider provider/scroll/UI run: 161 passed; four then-active settings implementation/preview fixture failures were reported to the root/UI owner.
- Typecheck after final type annotations: 0 errors, 2 existing ChatPane initial-controller warnings. Focused ESLint passed for owned implementation and tests after fixing test fixture typing; Prettier applied to owned frontend files and rustfmt to the new provider repository/facade modules.
- Full frontend integration run before the root's final UI sync: 862 passed, 9 failed and one scaling test transform failure introduced during lint cleanup. The scaling fixture was corrected and the focused suite passed; seven failures belonged to concurrently supplied settings UI/preview tests, and two strict pagination transports were handed to their owner. Root subsequently adopted newer UI fixtures and owns the final full gate.
- Storage batch regression passed before its size-neutral move to the model_routes child module: ordering and content equal existing nested list traversal, exactly one connection acquisition, archived connections omitted. A later rerun encountered another agent's temporary missing extraction test module; root's final Rust gate will validate the final integrated module location.
- Final measured parents (lines/bytes): app-controller.ts 562/21,603; CatalogPanel.svelte 727/26,685; DiscoveryPanel.svelte 1,211/49,846; demo-data.ts 1,132/39,542; Storage database.rs unchanged 890/33,656. Existing display/default helpers and fixed demo catalog fixtures moved to focused modules; no layout changes or cap increases.
- Shared native commands/contract size handling belongs to the runtime owner. Final public API inventory and source-cap ratchets belong to the root. No dependency changes, schema changes, commits, or writes to the original checkout were made by this area.

