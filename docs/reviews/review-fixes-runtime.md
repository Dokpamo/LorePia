# REVIEW-FIX-20260908 runtime work record

Before implementation: baseline snapshot `3c39090df2951673d4cc2c1ce044830b8287c3c5`, merge-base `6f22761a4305443541452e7409639114f4c10dfd`; isolated worktree `/Users/codexer/.codex/worktrees/lorepia-review-fixes-20260908` initially clean. The snapshot includes the user's original uncommitted work. The user authorized all review fixes and necessary behavior/API/policy changes. No dependencies or schema changes are planned.

Scope is report items 2, 3, and 7. Governing material: root AGENTS.md and ADRs 0001-0005. Public entries remain `lorepia_chat::run_generation`, native `lorepia-asset` registration and the existing resolver facade. No IPC additions are planned for these items.

- Chat: `crates/chat/src/generation.rs` owns the accumulator and `run_generation`; its inline tests contain `ProtocolProvider` and request fixtures. Change event work accounting so nonempty bounded text/reasoning/tool argument progress does not exhaust the separate 8,192-event flood bound. Preserve cumulative output/tool limits, credential reflection/zeroization, cancellation, sequence ordering, and inert tool protocol checks. Move the existing event budget test to a small nested child test module and add split-invariance and mixed empty/progress regressions. Parent expected to shrink; public constant name remains, with clarified semantics. Risk: an empty/control flood interleaved with valid text must still terminate.
- Assets: `apps/lorepia/src-tauri/src/asset_protocol.rs` owns descriptor/range validation and admission; `src/lib.rs` owns registration. Acquire bounded request work before descriptor verification, then reserve actual approved response bytes before reading the body; preserve both reservations through HTTP response conversion/consumption. Add a bounded wait for temporary admission contention instead of rejecting ordinary parallel avatar loads. Move admission implementation/tests to small child modules. Keep opaque canonical URI parsing, size/range/media checks, same-handle Storage verification and read, poisoning failure behavior, and 128 MiB aggregate body budget. Risk: waiting must not create unbounded tasks or memory, queue permits must be released on all failure paths, and response consumption must wake waiters. Any native lib.rs edits are restricted to the registration closure and coordinated with the IPC owner.
- Resolver: `crates/orchestration/src/resolver.rs::keep_latest_items` repeatedly estimates/removes a prefix. Move this helper to a small child module using a single reverse suffix scan and one drain. Keep exact retained messages, order, per-message overhead and golden output. Regression checks include 16,384 messages and estimator call count, boundary/zero budgets, and overflow-safe arithmetic. Parent expected to shrink. No changes to token estimator interface or serialized plans.

Baseline validation before edits to the relevant source: Chat library 45 passed; resolver tests 12 passed; cross-platform golden 1 passed; native asset protocol 6 passed. Rust is pinned to 1.96.0. Cargo commands use shared `CARGO_TARGET_DIR=/Users/codexer/lorepia/target` with team reservation. Full shared gate and baseline cap updates belong to root.

Asset retry extension before edits: read frontend AGENTS, TrustedAsset and its existing tests; baseline TrustedAsset tests pass (see execution log). Add at most two delayed media remount attempts (1s, 2s), using only the already validated exact URL; cancel timers on selector change/unmount. Unsafe descriptors and descriptor lookup failures stay fail-closed. Test successful retry, bounded exhaustion, and teardown. This is view-local presentation lifecycle, not shared request authority. No DTO, URL identity or sanitizer change.

Completed focused regressions for items 2/3/7: Chat 48 passed, resolver 14 and cross-platform golden 1 passed, native asset protocol 12 passed, TrustedAsset 22 passed (and Lua/regex worker posttests). Native request bodies are rejected before queueing because GET/HEAD media requests never consume them. Current native policy is four work/response slots, 128 queued requests, 30-second admission wait, and 128 MiB of actual bodies; the verified descriptor and same-handle read still govern allocation. The UI retries only already verified media URLs twice, at 1s and 2s.

## Same-task extension: review item 9 (before edits)

Root assigned Storage/Core presentation batching and normal-terminal incremental refresh as the next area of REVIEW-FIX-20260908. Read Core/Storage/frontend guides, ADRs 0001-0005, and inventory: `Core::list_branch_message_presentations -> present_messages -> Storage::get_message_display_projection`, Storage's recursive `list_branch_messages`, `get_message_display_projection` and `load_rule_diagnostics`; Shell conversation API; native `list_branch_messages`; `ChatStreamController::acceptStreamItem/reconcile`; existing generation_output and chat-stream tests. Target modules: Storage message_display_projection children and a narrow generation-message query child; Core message_display_projection and child; Shell conversation API; narrow native request/handler; renderer conversation client/contracts and chat stream child helper/tests. Mechanical single-command registration is coordinated with the pagination owner.

Add `list_generation_messages` using existing MessageDto[] (at most user+assistant) and strict conversation/branch/generation IDs. Read only a durable terminal generation's exact messages. UI checks current branch/head and parent continuity before replacing/appending that suffix; all mismatches, missing data, nonterminal state and stream-recovery reasons retain the full reconciliation path. Ordinary completion must not query or hash historical messages. Bulk projection queries use bounded ID batches and retain canonical/display/diagnostic hashes, generation ownership, stable diagnostic order and bounds. Storage keeps connection/query ownership; no Core SQL and no new public Core Stored* export.

Move projection loading/verification helpers into a small Storage child, preserve the existing single-message API through delegation, and extract a small frontend merge helper so parents stay within caps. Expected parent reduction in Storage; Core/frontend may grow below existing caps, without raising any baseline. Risks: wrong-generation or off-branch data must not append; terminal data must be read before publication; resumed/regenerated turns may already contain a pending assistant; stale UI responses must obey epochs. Baseline tests are recorded below before edits to each owner.

Item 9 baseline: frontend chat stream 18 passed; Core display-only terminal/reopen projection 1 passed; Storage projection 2 passed. Chat/Orchestration all-targets clippy passed for items 2/7. A shared-target stale Storage feature artifact was resolved by rebuilding the worktree source; subsequent Core baseline passed.

Batching refinement: each bounded 64-message query pair owns one connection guard, then releases it before hash/diagnostic verification. This avoids extending the SQLite critical section through a whole transcript. A 257-message regression expects five acquisitions; empty input acquires none. Existing pending-assistant lookup was moved to the terminal helper and now reverse-scans without cloning the transcript, keeping its public controller delegate and result unchanged.

Item 9 focused frontend validation: all 22 tests passed (18 existing chat-stream plus 4 terminal-refresh), including 10,000 existing messages with only one initial full history call, exact pair append, pending pair replacement, incorrect route/head/generation/parent rejection, and broadcast-lag full recovery. Lua and regex worker posttests passed. Storage projection suite passed 4 after bounded-lock refinement, including 257 missing sidecars across five acquisitions and 64 valid projections with input order and tampered content/ownership rejection.

Item 9 Core regressions passed 2: exact route lookup returns only the requested pair across three generations and matches full hash-verified DisplayOnly presentations; running output is unavailable until durable terminal completion. Native command request types for both existing message commands were moved together to the focused generation message module, retaining `contract` re-exports and old command names while shrinking both parent files. The additive command registry, strict handler, both capabilities, and generated permissions are synchronized. Frontend typecheck passed with zero errors and two pre-existing ChatPane initial-controller capture warnings; full npm check initially stopped at an unrelated TextEditor formatting change being integrated by root.

Final area validation: native strict generation route test passed 1 and asset protocol passed 12 again after command extraction. `cargo clippy -p lorepia-tauri --all-targets -- -D warnings` passed (including affected Storage/Core/Shell/Chat dependencies); a narrow `struct_field_names` expectation documents the existing IPC `*_id` convention on the new strict request. Scoped ESLint and Prettier passed all changed runtime frontend files. Full frontend lint found unrelated integrated UI issues that root is coordinating; the three initial test-only style violations here were fixed. Runtime items 2/3/7/9 are ready for integration and the full workspace gate; no commit, dependency/schema edit or source baseline increase was made.
