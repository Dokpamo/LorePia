# REVIEW-FIX-20260908 — greeting branch boundary

Baseline: review worktree snapshot `3c39090df2951673d4cc2c1ce044830b8287c3c5`, merge-base `6f22761a4305443541452e7409639114f4c10dfd`. Worktree contains the coordinated review fixes and preserved user UI changes; original checkout and live database are untouched.

Authorized addition: root relayed the live UI reproduction where creating a branch at a committed CHARX greeting fails with NotFound. Targets: Storage interaction checkpoint boundary resolution and focused Storage/Core regression tests; existing public APIs and schema remain unchanged. Public entry: Core/Storage `create_conversation_branch`; historical generation review also consumes the same boundary helper. No dependency, IPC, or migration change.

Owned invariants: only the exact persisted greeting identity may use the greeting boundary; current state may be used only while that greeting remains the exact source head, matching existing empty-branch semantics; after advancement only immutable before-generation evidence may restore history. Preserve canonical JSON and snapshot hashes, normalized state validation, branch/conversation ownership, pending-proposal rejection, and single transaction branch insertion/cloning. No blank or manufactured checkpoint.

Inventory: `clone_interaction_checkpoint_for_branch_transaction`, `read_generation_user_interaction_boundary`, `read_pre_first_message_interaction_boundary`, `read_generation_attempt_review_boundary`, greeting insertion and lifecycle initialization. Root greeting is persisted with a reserved provenance marker and exact ConversationStarted outbox head, but ConversationStarted has no owner message; therefore no message checkpoint exists and the generated-user fallback rejects it. Lifecycle event payload retains only a state hash, so it cannot reconstruct an arbitrary old snapshot.

Plan: baseline failing Core greeting fork regression; add a narrow exact greeting-boundary helper and extract shared historical-root decoding to keep existing checkpoint parent below its cap. Symbols remain named/delegated; no unrelated movement. Expected size delta: checkpoint parent shrinks; two focused children and tests. Semantic risks: confusing greeting provenance with generated messages, using live state for historical forks, or bypassing pending proposals. Governing material: repository/Storage/Core AGENTS, ADRs 0001–0003 and 0005, storage-public-api audit. Verify new greeting fork/reopen, exact nonempty state preservation, advanced-head failure without historical evidence, unbound marker rejection, and existing checkpoint tests.
