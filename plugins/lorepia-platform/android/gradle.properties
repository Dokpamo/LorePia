android.useAndroidX=true
org.gradle.dependency.verification=strict

# Tauri 2.11.5 still applies the external Kotlin plugin and legacy kotlinOptions.
# Keep AGP's documented compatibility mode until that pinned source migrates.
android.builtInKotlin=false
android.newDsl=false
